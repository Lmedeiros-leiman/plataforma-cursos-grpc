# Understanding the Project

Basically, the project is a box that contains several smaller boxes. The main big box is the scripting layer, which allows you to integrate and have the entire server running locally without much hassle.

To run everything, just use npm bro. check root package.json


